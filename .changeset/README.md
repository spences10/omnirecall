# Changesets

Run `pnpm changeset` for subsequent changes to the published CLI. Run
`pnpm run version` to apply those changesets before a release. The
first package version is already set to `0.0.1`.
